// Mock for nexus-module
const nexusModuleMock = {
  FieldSet: jest.fn(),
  Button: jest.fn(),
  TextField: jest.fn(),
  Select: jest.fn(),
  FormField: jest.fn(),
  Modal: jest.fn(),
  apiCall: jest.fn(),
  showErrorDialog: jest.fn(),
};

export default nexusModuleMock;